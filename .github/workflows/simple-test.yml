name: Simple Test

on:
  push:
    branches: ["1.0.14+1"]
  workflow_dispatch:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - name: Hello World
        shell: powershell
        run: Write-Output "Hello World! Pipeline is working!"
